[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nukeit"
version = "1.0.0"
description = "Network kill switch — nuke all interfaces and block all ports instantly"
requires-python = ">=3.8"

[project.scripts]
nukeit = "nukeit:main"
